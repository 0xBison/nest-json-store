// jest.config.js
module.exports = {
  testMatch: ["**/src/**/*.spec.ts"], // Match any .spec.ts files in the src directory
  preset: "ts-jest", // Use ts-jest preset
  testEnvironment: "node", // Set the test environment
};
